// Pure client-side SPA: no server-side rendering, no prerendering.
// Every route is served via the adapter-static `index.html` fallback and
// rendered in the browser.
export const ssr = false;
export const prerender = false;
